import express from 'express';
import { Router } from 'express';

const productRouter = Router();

productRouter.get(); 

productRouter.get(); // get all the products
productRouter.get(); // get a specific router

export default productRouter;